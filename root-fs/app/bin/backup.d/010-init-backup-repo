#!/bin/bash
if [[ ${1} != "--no-compress" ]] || [[ ! $(restic -r ${BLUESPICE_BACKUP_DIR} cat config) > /dev/null   ]]; then
	echo "Your Backup Password is: " ${BACKUP_PASS} > /proc/1/fd/1 2>&1 ; 
	restic -r /data/backup/ init <<< ${BACKUP_PASS} > /proc/1/fd/1 2>&1 ;
else
	mkdir -p /data/backup
fi
